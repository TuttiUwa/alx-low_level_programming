#include<stdio.h>
#include "main.h"
void _puts(char *s)
{
	puts(s);
}
/**
 * main - Entry point
 *
 * Return: Always 0 (Success)
 */
int main(void)
{
	puts("\"Programming is like building a mulitlingual puzzle");
	return (0);
}
